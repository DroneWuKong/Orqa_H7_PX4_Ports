#!/bin/sh
#
# board specific sensors init
# Orqa H7 QuadCore
#
# Sensor bring-up deferred to Phase 2
#------------------------------------------------------------------------------

board_adc start

# Sensor bring-up deferred to Phase 2
# The following commands are commented out to prevent startup crashes on Phase 1 firmware.
# Correct SPI bus numbers, chip select pins, and rotation constants will be
# determined and configured in Phase 2 (Sensors and Console).

# IMU 1 — ICM-42688P on SPI bus (TBD in Phase 2)
# icm42688p -s -b 1 -R <rotation> start

# IMU 2 — ICM-42688P on SPI bus (TBD in Phase 2)
# icm42688p -s -b 4 -R <rotation> start

# Barometer — DPS310 on I2C (TBD in Phase 2)
# dps310 -I start -a 118

# External mag (TBD in Phase 2)
# qmc5883l -X start -a 13
