#!/bin/sh
#
# ORQA H7 Wingcore board specific defaults
#------------------------------------------------------------------------------

# Transition from params file to flash-based params
if [ -f $PARAM_FILE ]
then
	param load $PARAM_FILE
	param save
	# create a backup
	mv $PARAM_FILE ${PARAM_FILE}.bak
	reboot
fi

# Battery voltage divider: 10K + 1.2K network
param set-default BAT1_V_DIV 11.0
param set-default BAT1_A_PER_V 92.6

# system_power unavailable
param set-default CBRK_SUPPLY_CHK 894281

# Select Generic Fixed Wing by default
param set-default SYS_AUTOSTART 2100

# Magnetometer on I2C1
param set-default SYS_HAS_MAG 1

# Enable gravity fusion for EKF2
param set-default EKF2_IMU_CTRL 7

# Buzzer
param set-default CBRK_BUZZER 0

param set-default IMU_GYRO_RATEMAX 2000

# Fixed-wing servo assignments on PWM9/PWM10
# 201=Aileron, 202=Elevator
param set-default PWM_MAIN_FUNC9 201
param set-default PWM_MAIN_FUNC10 202

# DShot for motor outputs
param set-default DSHOT_CONFIG 300
